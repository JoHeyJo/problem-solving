// "use strict";

// const { invertTree } = require('../invertTree');

// test("Given the root of a binary tree, invert the tree, and return its root..",
//   function () {
//     const result = invertTree([4, 2, 7, 1, 3, 6, 9]);
//     expect(result).toEqual([4, 7, 2, 9, 6, 3, 1]);
//   })

// test("Given the root of a binary tree, invert the tree, and return its root..",
//   function () {
//     const result = invertTree([2, 1, 3]);
//     expect(result).toEqual([2, 3, 1]);
//   })